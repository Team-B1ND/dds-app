import type { Colors } from '@dds-app/foundation/colors';
import type { Radius } from '@dds-app/foundation/radius';
import type { Opacity } from '@dds-app/foundation/opacity';
import type { Size } from '@dds-app/foundation/size';
import type { Spacing } from '@dds-app/foundation/spacing';
import type { Typography } from '@dds-app/foundation/typography';

export interface Themes {
  color: Colors;
  radius: Radius;
  opacity: Opacity;
  size: Size;
  spacing: Spacing;
  typography: Typography;
}
